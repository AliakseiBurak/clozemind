httpx>=0.27.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
itsdangerous>=2.2.0
markdown>=3.6.0

# tkinter is a system package — install with:
#   Ubuntu/Debian: sudo apt-get install python3-tk
#   Fedora:        sudo dnf install python3-tkinter
#   Arch:          sudo pacman -S tk
#   macOS:         comes with Python.org installer
#   Windows:       comes with the Windows installer
